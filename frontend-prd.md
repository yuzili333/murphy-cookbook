# Murphy's Cookbook 前端 PRD

## 1. 文档目的
本文档记录 Murphy's Cookbook 产品前端功能范围、页面结构、交互规则和验收标准，用于后续开发、联调、测试、演示和 CI/CD 验收。

产品形态已从传统多页面流程调整为 **儿童友好的 chatbox 食谱智能体**：用户通过文字、语音、拍照或上传图片提供食材，系统识别食材后让用户确认，再由大模型流式推荐适合小学阶段儿童的菜谱。当前版本同时包含中英文界面切换、拼音开关、食材小百科、菜名识字、Seedance 2.0 烹饪视频匹配播放，以及面向运营人员的菜谱视频配置页。

## 2. 产品定位
Murphy's Cookbook 是面向小学 1-6 年级儿童及家庭的儿童烹饪菜谱智能体。

核心目标：
- 用最短路径完成食材输入和菜谱推荐。
- 让儿童能通过图片、emoji、语音朗读和拼音参与理解。
- 让家长能快速判断食材、用量、步骤和安全风险。
- 在移动端浏览器中保持稳定、可读、不卡顿的 chatbox 体验。
- 支持中文和英文界面切换，并允许用户按需开启或关闭拼音/读音提示。

默认推荐原则：
- 默认服务对象为小学 1-6 年级学生。
- 推荐倾向：低油脂、轻口味、膳食均衡、维生素丰富、主食蛋白质蔬菜搭配均衡。
- 不默认要求用户填写儿童档案。
- 仅当用户明确提及重度急性过敏风险、过敏性休克、呼吸困难等场景时，主动提示补充过敏信息。

## 3. 当前产品总流程
1. 首次进入应用时展示故事板式开屏页，用户可选择中文或英文并点击进入。
2. 进入 chatbox 首页。
3. 系统展示欢迎语、时令食材建议和输入栏。
4. 用户通过文字、语音、拍照或上传图片提供食材。
5. 系统调用模型识别食材，识别期间展示 loading 且禁止继续添加。
6. 识别成功后展示横向食材清单，不自动推荐菜谱。
7. 用户可点击食材卡查看食材小百科，也可继续添加食材，或点击“推荐菜谱”。
8. 系统最多基于 10 个食材调用大模型流式推荐 1-2 道菜。
9. 菜谱以横向卡片列表展示，用户可在卡片内手动获取流式烹饪步骤，不跳转详情页。
10. 用户可朗读文本、收藏菜谱、查看历史对话、定位已收藏菜谱、打开菜名识字抽屉或播放已匹配的烹饪视频。
11. 运营人员可通过 `/cookbook-video-config` 登录菜谱视频配置页，维护已审核视频的名称、别名、食材、视频地址、封面地址、时长和分辨率。

## 4. 页面与信息架构

### 4.0 故事板式开屏页
页面目标：
在首次访问时用儿童厨房故事板快速建立产品心智，并让用户选择中文或英文后进入 chatbox。

核心模块：
- 品牌与儿童厨房故事视觉。
- 中英文切换按钮。
- 进入应用按钮。

关键交互：
- 用户点击进入后，记录开屏已关闭状态，后续刷新默认直接进入 chatbox。
- 用户在开屏页切换语言后，进入应用时沿用所选语言。

验收要点：
- 开屏页不能阻断老用户重复使用。
- 移动端布局不遮挡进入按钮。

### 4.1 单页 Chatbox 首页
页面目标：
承载食材输入、识别结果确认、菜谱推荐、菜谱详情阅读和对话历史。

核心模块：
- 顶部导航：历史对话入口、新对话按钮、菜谱收藏入口。
- 顶部语言切换：支持中文和英文界面切换。
- 儿童化欢迎区：卡通吉祥物、简短引导语。
- 聊天消息容器：展示用户输入、助手回复、上传图片预览、识别结果、推荐结果。
- 时令食材建议：按当前月份从服务端本地 300 条时令蔬果缓存中随机返回，匹配本地食材 emoji 时展示 emoji；接口异常时不展示。
- 输入栏：语音输入、文本输入、拍照、上传图片、发送按钮。
- 食材清单卡片：横向单行滚动展示食材。
- 菜谱卡片走马灯：横向滚动展示推荐菜谱。

验收要点：
- 首屏必须能直接开始输入食材。
- 用户无需先创建儿童档案。
- 输入、识别、推荐、详情阅读都在 chatbox 内完成。
- 移动端横向滚动食材或菜谱列表时，应尽量屏蔽浏览器左滑后退。

### 4.2 历史对话侧滑栏
页面目标：
保留用户本地对话信息，支持多轮对话和历史恢复。

核心模块：
- 最近对话列表。
- 新对话按钮。
- 当前对话高亮。

关键交互：
- 点击新对话时，重置当前 chatbox、食材清单、推荐结果和临时上下文。
- 点击历史对话时，恢复该对话的消息、食材和推荐卡片。
- 历史数据保存在浏览器本地存储。

验收要点：
- 新对话不会污染旧对话数据。
- 历史对话恢复后，仍可查看已有推荐卡片。

### 4.3 菜谱收藏侧滑栏
页面目标：
集中展示用户收藏过的菜谱。

核心模块：
- 收藏菜谱列表。
- 菜名、英文名和推荐耗时摘要。
- 空态提示。

关键交互：
- 点击已收藏菜谱后，关闭侧滑栏。
- 自动切换到原始对话并定位到对应菜谱卡片。

验收要点：
- 收藏入口位于页面右上角。
- 不再保留“用户设置”或完整“儿童档案”入口。

### 4.4 平板横屏辅助面板
页面目标：
在平板横屏等宽屏场景下提升历史对话和收藏菜谱的可见性，减少频繁打开侧滑栏。

核心模块：
- 左侧或辅助区域展示最近历史对话。
- 辅助区域展示最近收藏菜谱。
- 支持新建对话、切换历史对话、删除历史对话、打开收藏菜谱和移除收藏。

验收要点：
- 辅助面板只在适合的宽屏布局中展示，不影响移动端 chatbox 主流程。
- 操作结果与侧滑栏保持一致。

### 4.5 菜谱视频配置页
页面目标：
为运营或项目维护人员提供已审核烹饪视频的基础配置管理能力，使前端菜谱卡片可以稳定匹配并播放视频。

访问路径：
- `/cookbook-video-config`

核心模块：
- 管理员登录。
- 视频配置列表。
- 按菜谱名称关键词和分辨率筛选。
- 分页、编辑、新增、删除。
- 视频配置表单。

字段范围：
- 菜谱名称。
- 菜谱昵称。
- 食材清单。
- 视频地址。
- 封面地址。
- 视频时长。
- 分辨率：720p 或 1080p。

验收要点：
- 未登录或权限失效时必须提示重新登录。
- 新增和编辑时校验必填项、URL 格式、时长和分辨率。
- 本地开发环境可按配置允许调试绕过，生产环境必须校验管理员 token。

## 5. 核心功能需求

### 5.0 中英文界面与拼音开关
支持范围：
- 开屏页、顶部导航、chatbox 主流程、历史对话、收藏菜谱、食材小百科、菜名识字、视频卡槽和错误提示均支持中文/英文基础文案。
- 语言选择保存在 `localStorage`，刷新后保持用户选择。
- 历史对话抽屉内提供拼音/读音提示开关，控制菜名和识字相关读音提示的展示。

交互规则：
- 切换语言后，不清空当前对话、食材清单、推荐结果和收藏。
- 调用推荐、详情和食材知识接口时，将当前语言和拼音模式作为生成选项传给服务端。

验收要点：
- 中英文切换不会导致页面刷新或对话丢失。
- 拼音关闭后，核心菜谱信息仍完整可读。

### 5.1 多模态食材输入
支持输入方式：
- 文字输入。
- 浏览器语音输入。
- 拍摄照片。
- 上传本地图片。

交互规则：
- 文字或语音输入后，调用文本食材解析接口。
- 图片输入后，在 chatbox 中直接展示上传图片内容，而不是只展示文件名。
- 图片输入后，调用图片识别接口。
- 识别中展示 loading，禁用继续添加食材和推荐按钮。
- 识别成功后自动清空输入框。

验收要点：
- 移动端 Safari 中，输入框已有文字时仍能正常提交解析。
- 图片预览尺寸必须受控，不撑破聊天布局。

### 5.2 食材清单确认
页面目标：
让用户在推荐前确认识别出的食材。

展示要求：
- 食材清单采用单行横向滚动布局。
- 每个食材卡展示：本地 emoji、食材名称、拼音、朗读按钮、删除按钮。
- 点击食材卡时，在清单下方展示该食材的小百科卡片。
- 本地 300 种常见食材 emoji 匹配失败时，食材卡可使用默认占位；时令建议则不展示未命中的 emoji。
- 支持继续添加食材。
- 推荐上限为 10 个食材。

交互规则：
- 识别食材后不自动推荐菜谱。
- 用户可删除误识别食材。
- “添加食材”默认打开本地照片选择。
- 点击食材朗读按钮时播报食材名称和拼音。
- 点击食材卡调用食材知识接口，展示营养价值、产地、适宜气候、最佳搭配、儿童趣味说明和安全提示，并支持朗读。

验收要点：
- 清单为空时不能推荐菜谱。
- 超过 10 个食材时必须阻止推荐并给出提示。
- 横向滑动时不触发页面路由后退。

### 5.2.1 食材小百科
页面目标：
把食材确认环节扩展为轻量科普入口，让儿童在推荐菜谱前认识食材。

展示内容：
- 营养价值。
- 产地。
- 适宜气候。
- 最佳搭配。
- 儿童趣味说明。
- 安全提示。

技术规则：
- 接口：`POST /api/v1/ingredients/knowledge`。
- 请求包含食材名称、当前语言和拼音模式。
- 前端按食材名称和语言缓存结果，有效期 3 天。
- 获取失败时在卡片内展示失败提示和“重新获取”按钮。

验收要点：
- 点击不同食材时，小百科内容能切换。
- loading、失败、重试和朗读状态清晰可见。
- 小百科卡片不得挤压或破坏食材横滑列表。

### 5.3 推荐菜谱
推荐触发：
- 用户点击“推荐菜谱”按钮后才调用推荐接口。
- 当前前端优先使用流式推荐接口，边生成文本边等待菜谱卡片数据。

推荐数量：
- 每次推荐 1-2 道菜，降低首屏等待时间并保证卡片内容可读。

推荐约束：
- 默认适配小学阶段儿童。
- 推荐简单易上手的菜谱。
- 涉及明火、天然气、电磁炉、微波炉、烤箱、空气炸锅、蒸锅、热锅、热油、开水或刀具时，必须高亮提示需家长陪同。
- 对同一份食材清单再次推荐时，返回新推荐数据后清空上一轮相同食材清单的推荐结果，仅展示最新推荐。

按钮状态：
- 未推荐时，“推荐菜谱”按钮可使用轻量彩虹呼吸灯吸引注意。
- 成功推荐过相同食材清单后，按钮停止闪烁，保持静态正常态。
- 按钮文字必须保持清晰可读，动效不得干扰阅读。
- 点击按钮进入 loading 时，chatbox 容器位置保持不滚动。
- 推荐数据返回并渲染菜谱卡片后，将菜谱卡片列表顶部保持在用户视口中，用户自行决定是否继续下滑浏览详情。

验收要点：
- 推荐前不自动触发大模型。
- loading 不造成聊天容器异常跳动。
- 推荐结果回来后，不直接滚到底部。

### 5.4 菜谱卡片展示
展示形态：
- 菜谱推荐以横向走马灯卡片展示。
- 卡片内直接展示详情，不跳转页面路由。
- 不展示菜谱预览图，避免无可靠图床或本地食谱素材导致误导。
- 支持在菜谱卡片内展示菜谱烹饪视频卡槽。
- 烹饪步骤默认不强制自动展开；当详情未生成时，卡片内展示“获取烹饪步骤”按钮。

卡片信息：
- 中文菜名。
- 带调号拼音。
- 英文菜名。
- 营养摘要。
- 难度、适合年龄、预计时长。
- 烹饪注意事项。
- 食材配料清单和用量。
- 详尽烹饪步骤。
- 每一步的完成状态、小贴士和家长协助提示。

交互：
- 中文菜名可朗读。
- 英文菜名可朗读。
- 营养摘要和烹饪注意事项可朗读。
- 每个食材用量可朗读。
- 每个步骤可朗读。
- 步骤朗读过程中再次点击朗读按钮，应立即停止播报。
- 菜谱可收藏。
- 菜谱卡片命中烹饪视频资源时，可在浏览器端点击播放视频。
- 获取烹饪步骤时优先使用流式详情接口，在卡片内展示生成中的文本节点；生成失败时显示失败提示和“再次获取”按钮。

验收要点：
- 食材名称和用量分别清晰展示，不得出现竖排文字。
- 高风险步骤必须有明显的家长陪同提示。
- 菜谱卡片左滑时不触发移动端浏览器后退。

### 5.5 Seedance 2.0 菜谱烹饪视频功能
需求背景：
菜谱烹饪视频用于帮助儿童和家长快速理解烹饪流程、食材状态变化和最终菜品形态。视频制作方式参考 `video-design/seedance-prompt-skill/SKILL.md` 中的 Seedance 2.0 菜谱烹饪类动画视频提示词与故事板设计规范，将大模型生成的烹饪步骤进一步转化为儿童友好的短视频视觉素材。

制作方式：
- 因视频模型生成成本高、渲染耗时长、生成内容需要人工审核，产品不在普通用户请求链路中实时生成视频。
- 烹饪视频采用“AI 辅助离线生成 + 人工审核 + 服务端登记 + 前端匹配播放”的方式。
- 视频生产人员可基于菜谱详情中的步骤、食材清单、风险提醒和成品名称，按 Seedance 2.0 模板生成 storyboard 图片提示词和视频生成提示词。
- storyboard 默认采用 3x3 九宫格结构：食材亮相、清洗/切配、调味/搅拌、热锅、主食材下锅、翻炒/炖煮/煎烤、质感特写、装盘、成品英雄镜头。
- 视频建议时长 12-15 秒，横屏 16:9 优先，风格为温暖、干净、阳光、儿童友好的高质量卡通/动画电影厨房风格。
- 当前版本视频配置页登记菜谱名称、菜谱昵称清单、食材清单、视频地址、封面地址、时长和分辨率。Seedance 2.0 生成提示词、storyboard 提示词和审核记录由线下制作文档或项目资料保管，不进入当前前端配置表单。
- 同一道菜可能存在多种叫法，例如“番茄炒蛋”“西红柿炒鸡蛋”“番茄鸡蛋”等，必须通过昵称清单统一映射。

Seedance 2.0 提示词要求：
- 每条视频只聚焦一道菜，不把多道菜合并生成。
- 提示词必须包含画幅、时长、菜品主题、烹饪流程、整体风格、厨房场景、角色或手部设定、镜头设计、食物质感、声音氛围、一致性要求和负向约束。
- 烹饪流程必须来源于当前菜谱详情步骤，不得加入菜谱步骤外的主食材或改变菜品名称。
- storyboard 图片内不放文字、编号、字幕、Logo、水印或 UI；关键步骤文字只写在配套视频提示词中。
- 视频画面需要体现真实连贯的手部动作、锅具运动、蒸汽、油光、汁水、火焰和食材质感。
- 必须加入负向约束：不要生成静态图片 Slides；无文字、无字幕、无 Logo、无水印；避免手部变形、刀具穿模、锅具漂移、食材融化、动作不符合物理、镜头间风格突变。

匹配规则：
- 前端在渲染 chatbox 菜谱卡片时，按菜谱中文名称查询已上传视频资源。
- 只要菜谱中文名称命中某条视频资源的 `recipeName` 或 `recipeAliases` 任意一项，即判定为该菜谱存在烹饪视频。
- 匹配需做基础标准化：去除首尾空格、忽略中英文大小写、允许常见中文全半角差异。
- 当前版本不基于英文名、食材相似度或大模型语义相似度做视频匹配，避免误播。

展示规则：
- 菜谱卡片内保留固定视频卡槽。
- 命中视频时，展示视频封面、播放按钮、视频时长和“烹饪视频”标签。
- 用户点击播放后，在浏览器端使用原生 HTML5 video 播放。
- 未命中视频时，仍保留占位符视频卡槽，当前实现显示“未查询到菜谱烹饪视频”。
- 视频卡槽不得挤压菜谱基础信息和烹饪步骤内容。

验收要点：
- 已上传并审核通过的视频能在对应菜谱卡片中展示并播放。
- 菜谱名称命中任一昵称即可匹配视频。
- 未匹配到视频的菜谱卡片必须显示未查询到视频的占位状态。
- 视频加载失败时，卡槽内展示失败提示，不影响菜谱文字内容阅读。
- 当前线上配置资源必须可追溯到菜谱名称、昵称、食材、视频地址、封面地址、时长、分辨率和更新时间；Seedance 2.0 生成提示词、storyboard 提示词和审核过程由线下项目资料追溯。
- 视频内容不得出现字幕、Logo、水印、错误菜名、错误食材或与菜谱步骤明显不一致的画面。

### 5.6 语音朗读
朗读范围：
- 用户消息。
- 助手消息。
- 食材名称。
- 中文菜名。
- 英文菜名。
- 菜谱摘要和提示信息。
- 食材用量。
- 烹饪步骤。
- 菜名识字内容。

技术方案：
- 当前版本使用浏览器 Web Speech API。
- 不单独建设服务端 TTS。

交互规则：
- 如果正在朗读同一段内容，再次点击朗读按钮应立即停止。
- 关闭识字抽屉或相关弹层时，应立即停止播报。
- 浏览器不支持语音时，给出提示，文本内容仍应可见。

## 6. 菜名识字
需求背景：
推荐菜谱不仅用于“吃什么”的决策，也承担儿童参与和学习兴趣引导。当前版本在不引入课程系统的前提下，将菜谱名称作为轻量识字入口。

功能范围：
- 大模型推荐菜谱时，除中文菜谱名外，必须返回自然英文菜名。
- 大模型必须返回中文菜名中每个汉字的识字信息，包括：单字、拼音、笔画数、文字结构、儿童化说明。
- 拼音必须使用带调号拼音，不得显示数字声调。
- 识字详情不默认展示。
- 点击中文菜名后，从侧边抽屉展示识字内容。
- 抽屉内每个字支持点击朗读。
- 关闭抽屉时立即停止播报。
- 英文菜名点击后按英文朗读。

暂不包含：
- 不要求笔顺动画。
- 不要求离线语音包。
- 不要求逐字高亮跟读。

## 7. 大模型与接口要求

### 7.1 食材理解
- 文本解析接口：`POST /api/v1/ingredients/parse-text`
- 图片识别接口：`POST /api/v1/ingredients/recognize-image`
- 时令建议接口：`POST /api/v1/ingredients/seasonal-suggestions`
- 食材知识接口：`POST /api/v1/ingredients/knowledge`

要求：
- 图片识别必须使用真实大模型接口，生产环境未配置模型时返回明确错误。
- 时令建议不调用大模型，按当前月份从本地时令蔬果缓存随机返回 3 个建议；接口异常时前端静默隐藏建议清单。
- 食材知识可调用大模型生成，必须返回营养价值、产地、适宜气候、最佳搭配、儿童趣味说明和安全提示。

### 7.2 菜谱推荐
- 推荐接口：`POST /api/v1/recommendations/recipes`
- 流式推荐接口：`POST /api/v1/recommendations/recipes/stream`

输入：
- 当前食材清单。
- 默认小学阶段儿童画像。
- 用户对话中补充的特殊饮食信息。

输出：
- 推荐菜谱列表。
- 可选的预生成菜谱详情列表。
- 中文名、英文名和识字信息。
- 营养摘要、风险提醒。
- 难度、时间、适龄字段。
- 流式接口通过 SSE 返回文本增量和 `recipe-card` 卡片事件。

### 7.3 菜谱详情
- 详情接口：`POST /api/v1/recipes/detail`
- 流式详情接口：`POST /api/v1/recipes/detail/stream`

要求：
- 返回完整食材用量和详细步骤。
- 每个步骤必须包含：标题、描述、小贴士、儿童动作、家长动作、完成状态、风险等级、是否需要家长陪同。
- 前端需按菜谱名称将已生成的烹饪步骤详情缓存到 `localStorage`，有效期 3 天，重复访问同名菜谱时优先读取本地缓存。
- 部署环境中应通过模型路由、提示词收敛、超时降级和失败重试降低耗时。
- 接口异常时前端必须展示 toast、卡片内失败提示和“再次获取”按钮。
- 流式详情接口通过 SSE 返回文本增量和详情卡片数据，前端在菜谱卡片内展示生成状态。

### 7.4 Seedance 2.0 菜谱视频资源
资源查询接口：
- `POST /api/v1/recipe-videos/match`

输入：
- 当前菜谱中文名称。
- 可选：菜谱英文名称、当前食材清单。

输出：
- 匹配成功时返回单条已审核视频资源。
- 未匹配时返回空结果，前端显示占位符。

服务端匹配要求：
- 仅匹配审核通过的视频资源。
- 使用 `recipeName` 和 `recipeAliases` 做确定性匹配。
- 不调用大模型做动态视频生成或语义匹配。

视频上传登记要求：
- 当前版本上传方需登记 `recipeName`、`recipeAliases`、`ingredients`、`videoUrl`、`coverUrl`、`durationSeconds`、`resolution`。
- 服务端写入的视频配置状态固定为 `approved`，前端只播放已返回的视频资源。
- 视频文件由人工离线制作、人工审核并上传，制作过程参考 `video-design/seedance-prompt-skill/SKILL.md`。
- 服务端不提供用户侧动态生成视频能力。
- 后台或脚本侧可基于菜谱详情生成 Seedance 2.0 storyboardPrompt 和 videoPrompt，但当前配置页不保存提示词正文，提示词和审核记录需在线下项目资料中留存。

### 7.5 菜谱视频配置管理
- 登录接口：`POST /api/v1/video-config/auth`
- 列表接口：`GET /api/v1/video-config/recipes`
- 新增接口：`POST /api/v1/video-config/recipes`
- 更新接口：`PUT /api/v1/video-config/recipes/:id`
- 删除接口：`DELETE /api/v1/video-config/recipes/:id`

要求：
- 登录成功后返回 bearer token 和 `video_config_manage` 权限。
- 列表接口支持关键词、分辨率、分页、排序。
- 新增和更新接口必须校验菜谱名称、昵称、视频地址、封面地址、时长和分辨率。
- 存储优先使用 MongoDB 配置；未配置时使用本地 `.local/recipe-videos.json` 文件。

## 8. 本地状态与数据保留
本地保留内容：
- 当前对话消息。
- 历史对话列表。
- 当前对话食材清单。
- 已收藏菜谱。
- 最近推荐结果。
- 菜谱步骤详情缓存。
- 菜谱视频资源匹配缓存。
- 食材知识缓存。
- 语言选择。
- 拼音/读音提示开关。
- 开屏页关闭状态。
- 用户上传图片在聊天消息中的本地预览数据。
- 视频配置页管理员 token 使用 `sessionStorage` 临时保留。

存储方式：
- 当前版本使用浏览器 `localStorage`。
- 对话列表最多保留最近 30 条会话。
- 单个会话消息最多保留最近 40 条。
- 推荐结果、菜谱步骤详情和菜谱视频匹配缓存有效期为 3 天。
- 食材知识缓存有效期为 3 天。

验收要点：
- 刷新页面后，历史对话和收藏可恢复。
- 新对话必须清空当前临时状态。

## 9. 视觉与移动端体验

视觉方向：
- Neon green + orange 主题。
- 儿童友好、明亮、活泼。
- 顶部使用卡通厨师吉祥物。
- 首次访问展示故事板式开屏页，进入后回到 chatbox 主体验。
- 按钮和卡片可使用轻量 bounce、glow、pop-in 动效。

约束：
- 动效不能干扰阅读。
- 重要文字必须保持足够对比度。
- 卡片不使用不可靠菜谱图片。
- 图片输入只展示用户上传图片本身。
- 所有文本不得溢出容器或互相遮挡。

移动端要求：
- chatbox 输入栏固定在底部。
- 时令建议悬浮在输入框上方，不随聊天窗口滚动。
- 食材列表和菜谱列表横滑时，应减少浏览器后退手势误触。
- 推荐按钮点击后不滚动页面；推荐结果返回后只把菜谱列表顶部带入视口。

## 10. 错误与加载状态
必须覆盖：
- 食材解析 loading。
- 图片上传识别 loading。
- 推荐菜谱 loading。
- 菜谱详情 loading。
- 菜谱详情失败。
- 食材知识 loading 和失败重试。
- 菜谱视频匹配 loading 和未命中状态。
- 视频配置页登录失败、权限失效、列表读取失败和表单校验失败。
- 模型接口异常。
- 浏览器不支持语音。
- 食材超出 10 个上限。
- 未识别到有效食材。

前端行为：
- loading 期间禁用重复触发。
- 接口异常给出明确提示。
- 菜谱详情失败时提供“再次获取”。
- 时令建议接口异常时静默隐藏建议，不打断主流程。

## 11. 数据模型摘要

### 11.1 IngredientItem
- `id`
- `name`
- `normalizedName`
- `quantity`
- `source`
- `confidence`

### 11.2 RecipeRecommendation
- `id`
- `name`
- `namePinyin`
- `englishName`
- `nameLearning`
- `ageRange`
- `difficulty`
- `estimatedTimeMinutes`
- `riskAlerts`
- `nutritionSummary`
- `canCookWithCurrentIngredients`
- `cookingVideo`

### 11.3 RecipeDetail
继承 `RecipeRecommendation`，并增加：
- `prepTimeMinutes`
- `cookTimeMinutes`
- `ingredients`
- `steps`

### 11.4 RecipeCookingVideo
- `id`
- `recipeName`
- `recipeAliases`
- `ingredients`
- `videoUrl`
- `coverUrl`
- `durationSeconds`
- `resolution`
- `status`
- `createdAt`
- `updatedAt`

### 11.5 CookingStep
- `id`
- `title`
- `description`
- `tip`
- `childAction`
- `parentAction`
- `expectedResult`
- `riskLevel`
- `requiresParentAssist`

### 11.6 ChatMessage
- `id`
- `role`
- `text`
- `createdAt`
- `imageDataUrl`
- `imageAlt`
- `ingredientsKey`
- `ingredients`
- `recipes`

### 11.7 IngredientKnowledge
- `name`
- `nutritionValues`
- `origin`
- `growingClimate`
- `bestPairings`
- `kidFact`
- `safetyNote`

### 11.8 StreamEvent / MessageNode
- 流式接口返回文本增量、卡片事件、错误事件和完成事件。
- 前端将流式文本转成消息节点展示，卡片事件用于挂载菜谱推荐或详情数据。

## 12. 开发优先级
P0：
- 故事板式开屏页。
- 中英文界面切换。
- 拼音/读音提示开关。
- Chatbox 主流程。
- 多模态食材输入。
- 食材清单确认。
- 食材小百科。
- 推荐菜谱。
- 流式推荐和流式详情生成。
- 卡片内菜谱详情。
- 语音朗读。
- 菜谱收藏。
- 历史对话。
- 菜谱详情失败重试。
- Seedance 2.0 菜谱烹饪视频卡槽、已上传视频匹配和浏览器播放。
- 菜谱视频配置页基础管理。

P1：
- 更精细的移动端手势体验。
- 更稳定的中文字典校验。
- 菜谱详情性能监控。
- Seedance 2.0 视频提示词生成辅助脚本或管理后台。
- 菜谱视频配置页保存 Seedance 2.0 提示词、storyboard 提示词、负向提示词和审核记录。
- 收藏菜谱跨设备同步。

P2：
- 笔顺动画。
- 成果点评增强。
- 账号体系。
- 社区分享。
- 成长体系。

## 13. 暂不包含范围
- 不建设完整儿童档案管理模块。
- 不建设用户设置中心。
- 不建设服务端 TTS。
- 不建设真实菜谱图床。
- 不建设用户侧实时动态生成菜谱烹饪视频能力。
- 不在当前视频配置页保存 Seedance 2.0 提示词正文和审核流水。
- 不建设社区或公开分享。
- 不要求跨设备同步历史对话。
- 不要求离线模型能力。

## 14. 交付验收清单
- 首次访问展示故事板式开屏页，进入后刷新不重复阻断主流程。
- 用户可切换中文/英文界面，切换后对话数据不丢失。
- 用户可在历史对话抽屉中开启或关闭拼音/读音提示。
- 用户能通过文字、语音、拍照和上传图片输入食材。
- 图片输入能在 chatbox 中展示图片预览。
- 识别食材后不会自动推荐菜谱。
- 食材清单支持横向滚动、朗读、删除和继续添加。
- 点击食材卡可展示食材小百科，并支持失败重试和朗读。
- 推荐按钮点击后不导致 chatbox 立即滚动。
- 推荐结果返回后，菜谱列表顶部位于用户视口内。
- 推荐和菜谱详情可使用流式生成状态展示。
- 菜谱详情直接展示在卡片中。
- 菜谱无预览图。
- 菜谱卡片有固定 Seedance 2.0 烹饪视频卡槽。
- 命中已审核视频资源时，用户可在浏览器端点击播放。
- 未命中视频资源时，视频卡槽显示未查询到视频的占位状态。
- 已审核视频资源需登记菜谱名称、昵称、食材、视频地址、封面地址、时长和分辨率。
- `/cookbook-video-config` 可登录后新增、编辑、筛选、分页查看和删除菜谱视频配置。
- 菜谱中文名和英文名均可朗读。
- 步骤可朗读，并支持再次点击停止。
- 高风险步骤必须提示家长陪同。
- 菜谱详情失败时有 toast、失败提示和再次获取按钮。
- 历史对话和收藏菜谱可本地恢复。
- 移动端横滑食材和菜谱列表时尽量不触发浏览器后退。
