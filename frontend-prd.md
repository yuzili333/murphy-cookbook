# Murphy's Cookbook 前端 MVP PRD

## 1. 文档目的
本文档记录 Murphy's Cookbook 当前版本的前端 MVP 功能范围、页面结构、交互规则和验收标准，用于后续开发、联调、测试和 CI/CD 验收。

当前 MVP 已从传统多页面流程调整为 **儿童友好的 chatbox 食谱智能体**：用户通过文字、语音、拍照或上传图片提供食材，系统识别食材后让用户确认，再由大模型推荐适合小学阶段儿童的菜谱。

## 2. 产品定位
Murphy's Cookbook 是面向小学 1-6 年级儿童及家庭的儿童烹饪菜谱智能体。

核心目标：
- 用最短路径完成食材输入和菜谱推荐。
- 让儿童能通过图片、emoji、语音朗读和拼音参与理解。
- 让家长能快速判断食材、用量、步骤和安全风险。
- 在移动端浏览器中保持稳定、可读、不卡顿的 chatbox 体验。

默认推荐原则：
- 默认服务对象为小学 1-6 年级学生。
- 推荐倾向：低油脂、轻口味、膳食均衡、维生素丰富、主食蛋白质蔬菜搭配均衡。
- 不默认要求用户填写儿童档案。
- 仅当用户明确提及重度急性过敏风险、过敏性休克、呼吸困难等场景时，主动提示补充过敏信息。

## 3. 当前 MVP 总流程
1. 进入 chatbox 首页。
2. 系统展示欢迎语、时令食材建议和输入栏。
3. 用户通过文字、语音、拍照或上传图片提供食材。
4. 系统调用模型识别食材，识别期间展示 loading 且禁止继续添加。
5. 识别成功后展示横向食材清单，不自动推荐菜谱。
6. 用户选择继续添加食材，或点击“推荐菜谱”。
7. 系统最多基于 10 个食材调用大模型推荐 1-2 道菜。
8. 菜谱以横向卡片列表展示，详情内容直接在卡片内展示，不跳转详情页。
9. 用户可朗读文本、收藏菜谱、查看历史对话或定位已收藏菜谱。

## 4. 页面与信息架构

### 4.1 单页 Chatbox 首页
页面目标：
承载食材输入、识别结果确认、菜谱推荐、菜谱详情阅读和对话历史。

核心模块：
- 顶部导航：历史对话入口、新对话按钮、菜谱收藏入口。
- 儿童化欢迎区：卡通吉祥物、简短引导语。
- 聊天消息容器：展示用户输入、助手回复、上传图片预览、识别结果、推荐结果。
- 时令食材建议：按当前月份从服务端本地 300 条时令蔬果缓存中随机返回，匹配本地食材 emoji 时展示 emoji；接口异常时不展示。
- 输入栏：语音输入、文本输入、拍照、上传图片、发送按钮。
- 食材清单卡片：横向单行滚动展示食材。
- 菜谱卡片走马灯：横向滚动展示推荐菜谱。

验收要点：
- 首屏必须能直接开始输入食材。
- 用户无需先创建儿童档案。
- 输入、识别、推荐、详情阅读都在 chatbox 内完成。
- 移动端横向滚动食材或菜谱列表时，应尽量屏蔽浏览器左滑后退。

### 4.2 历史对话侧滑栏
页面目标：
保留用户本地对话信息，支持多轮对话和历史恢复。

核心模块：
- 最近对话列表。
- 新对话按钮。
- 当前对话高亮。

关键交互：
- 点击新对话时，重置当前 chatbox、食材清单、推荐结果和临时上下文。
- 点击历史对话时，恢复该对话的消息、食材和推荐卡片。
- 历史数据保存在浏览器本地存储。

验收要点：
- 新对话不会污染旧对话数据。
- 历史对话恢复后，仍可查看已有推荐卡片。

### 4.3 菜谱收藏侧滑栏
页面目标：
集中展示用户收藏过的菜谱。

核心模块：
- 收藏菜谱列表。
- 菜名、英文名和推荐耗时摘要。
- 空态提示。

关键交互：
- 点击已收藏菜谱后，关闭侧滑栏。
- 自动切换到原始对话并定位到对应菜谱卡片。

验收要点：
- 收藏入口位于页面右上角。
- 不再保留“用户设置”或“儿童档案”入口。

## 5. 核心功能需求

### 5.1 多模态食材输入
支持输入方式：
- 文字输入。
- 浏览器语音输入。
- 拍摄照片。
- 上传本地图片。

交互规则：
- 文字或语音输入后，调用文本食材解析接口。
- 图片输入后，在 chatbox 中直接展示上传图片内容，而不是只展示文件名。
- 图片输入后，调用图片识别接口。
- 识别中展示 loading，禁用继续添加食材和推荐按钮。
- 识别成功后自动清空输入框。

验收要点：
- 移动端 Safari 中，输入框已有文字时仍能正常提交解析。
- 图片预览尺寸必须受控，不撑破聊天布局。

### 5.2 食材清单确认
页面目标：
让用户在推荐前确认识别出的食材。

展示要求：
- 食材清单采用单行横向滚动布局。
- 每个食材卡展示：本地 emoji、食材名称、拼音、朗读按钮、删除按钮。
- 本地 300 种常见食材 emoji 匹配失败时，食材卡可使用默认占位；时令建议则不展示未命中的 emoji。
- 支持继续添加食材。
- 推荐上限为 10 个食材。

交互规则：
- 识别食材后不自动推荐菜谱。
- 用户可删除误识别食材。
- “添加食材”默认打开本地照片选择。
- 点击食材朗读按钮时播报食材名称和拼音。

验收要点：
- 清单为空时不能推荐菜谱。
- 超过 10 个食材时必须阻止推荐并给出提示。
- 横向滑动时不触发页面路由后退。

### 5.3 推荐菜谱
推荐触发：
- 用户点击“推荐菜谱”按钮后才调用推荐接口。

推荐数量：
- 每次推荐 1-2 道菜，降低首屏等待时间并保证卡片内容可读。

推荐约束：
- 默认适配小学阶段儿童。
- 推荐简单易上手的菜谱。
- 涉及明火、天然气、电磁炉、微波炉、烤箱、空气炸锅、蒸锅、热锅、热油、开水或刀具时，必须高亮提示需家长陪同。
- 对同一份食材清单再次推荐时，返回新推荐数据后清空上一轮相同食材清单的推荐结果，仅展示最新推荐。

按钮状态：
- 未推荐时，“推荐菜谱”按钮可使用轻量彩虹呼吸灯吸引注意。
- 成功推荐过相同食材清单后，按钮停止闪烁，保持静态正常态。
- 按钮文字必须保持清晰可读，动效不得干扰阅读。
- 点击按钮进入 loading 时，chatbox 容器位置保持不滚动。
- 推荐数据返回并渲染菜谱卡片后，将菜谱卡片列表顶部保持在用户视口中，用户自行决定是否继续下滑浏览详情。

验收要点：
- 推荐前不自动触发大模型。
- loading 不造成聊天容器异常跳动。
- 推荐结果回来后，不直接滚到底部。

### 5.4 菜谱卡片展示
展示形态：
- 菜谱推荐以横向走马灯卡片展示。
- 卡片内直接展示详情，不跳转页面路由。
- 不展示菜谱预览图，避免无可靠图床或本地食谱素材导致误导。

卡片信息：
- 中文菜名。
- 带调号拼音。
- 英文菜名。
- 营养摘要。
- 难度、适合年龄、预计时长。
- 烹饪注意事项。
- 食材配料清单和用量。
- 详尽烹饪步骤。
- 每一步的完成状态、小贴士和家长协助提示。

交互：
- 中文菜名可朗读。
- 英文菜名可朗读。
- 营养摘要和烹饪注意事项可朗读。
- 每个食材用量可朗读。
- 每个步骤可朗读。
- 步骤朗读过程中再次点击朗读按钮，应立即停止播报。
- 菜谱可收藏。

验收要点：
- 食材名称和用量分别清晰展示，不得出现竖排文字。
- 高风险步骤必须有明显的家长陪同提示。
- 菜谱卡片左滑时不触发移动端浏览器后退。

### 5.5 语音朗读
朗读范围：
- 用户消息。
- 助手消息。
- 食材名称。
- 中文菜名。
- 英文菜名。
- 菜谱摘要和提示信息。
- 食材用量。
- 烹饪步骤。
- 菜名识字内容。

技术方案：
- MVP 使用浏览器 Web Speech API。
- 不单独建设服务端 TTS。

交互规则：
- 如果正在朗读同一段内容，再次点击朗读按钮应立即停止。
- 关闭识字抽屉或相关弹层时，应立即停止播报。
- 浏览器不支持语音时，给出提示，文本内容仍应可见。

## 6. 菜名识字 MVP
需求背景：
推荐菜谱不仅用于“吃什么”的决策，也承担儿童参与和学习兴趣引导。MVP 在不引入课程系统的前提下，将菜谱名称作为轻量识字入口。

MVP 范围：
- 大模型推荐菜谱时，除中文菜谱名外，必须返回自然英文菜名。
- 大模型必须返回中文菜名中每个汉字的识字信息，包括：单字、拼音、笔画数、文字结构、儿童化说明。
- 拼音必须使用带调号拼音，不得显示数字声调。
- 识字详情不默认展示。
- 点击中文菜名后，从侧边抽屉展示识字内容。
- 抽屉内每个字支持点击朗读。
- 关闭抽屉时立即停止播报。
- 英文菜名点击后按英文朗读。

非 MVP 范围：
- 不要求笔顺动画。
- 不要求离线语音包。
- 不要求逐字高亮跟读。
- 不要求多语言切换。

## 7. 大模型与接口要求

### 7.1 食材理解
- 文本解析接口：`POST /api/v1/ingredients/parse-text`
- 图片识别接口：`POST /api/v1/ingredients/recognize-image`
- 时令建议接口：`POST /api/v1/ingredients/seasonal-suggestions`

要求：
- 图片识别必须使用真实大模型接口，生产环境未配置模型时返回明确错误。
- 时令建议不调用大模型，按当前月份从本地时令蔬果缓存随机返回 3 个建议；接口异常时前端静默隐藏建议清单。

### 7.2 菜谱推荐
- 推荐接口：`POST /api/v1/recommendations/recipes`

输入：
- 当前食材清单。
- 默认小学阶段儿童画像。
- 用户对话中补充的特殊饮食信息。

输出：
- 推荐菜谱列表。
- 中文名、英文名和识字信息。
- 营养摘要、风险提醒。
- 难度、时间、适龄字段。

### 7.3 菜谱详情
- 详情接口：`POST /api/v1/recipes/detail`

要求：
- 返回完整食材用量和详细步骤。
- 每个步骤必须包含：标题、描述、小贴士、儿童动作、家长动作、完成状态、风险等级、是否需要家长陪同。
- 前端需按菜谱名称将已生成的烹饪步骤详情缓存到 `localStorage`，有效期 3 天，重复访问同名菜谱时优先读取本地缓存。
- 部署环境中应通过模型路由、提示词收敛、超时降级和失败重试降低耗时。
- 接口异常时前端必须展示 toast、卡片内失败提示和“再次获取”按钮。

## 8. 本地状态与数据保留
本地保留内容：
- 当前对话消息。
- 历史对话列表。
- 当前对话食材清单。
- 已收藏菜谱。
- 最近推荐结果。
- 菜谱步骤详情缓存。
- 用户上传图片在聊天消息中的本地预览数据。

存储方式：
- MVP 使用浏览器 `localStorage`。
- 对话列表最多保留最近 30 条会话。
- 单个会话消息最多保留最近 40 条。
- 推荐结果和菜谱步骤详情缓存有效期为 3 天。

验收要点：
- 刷新页面后，历史对话和收藏可恢复。
- 新对话必须清空当前临时状态。

## 9. 视觉与移动端体验

视觉方向：
- Neon green + orange 主题。
- 儿童友好、明亮、活泼。
- 顶部使用卡通厨师吉祥物。
- 按钮和卡片可使用轻量 bounce、glow、pop-in 动效。

约束：
- 动效不能干扰阅读。
- 重要文字必须保持足够对比度。
- 卡片不使用不可靠菜谱图片。
- 图片输入只展示用户上传图片本身。
- 所有文本不得溢出容器或互相遮挡。

移动端要求：
- chatbox 输入栏固定在底部。
- 时令建议悬浮在输入框上方，不随聊天窗口滚动。
- 食材列表和菜谱列表横滑时，应减少浏览器后退手势误触。
- 推荐按钮点击后不滚动页面；推荐结果返回后只把菜谱列表顶部带入视口。

## 10. 错误与加载状态
必须覆盖：
- 食材解析 loading。
- 图片上传识别 loading。
- 推荐菜谱 loading。
- 菜谱详情 loading。
- 菜谱详情失败。
- 模型接口异常。
- 浏览器不支持语音。
- 食材超出 10 个上限。
- 未识别到有效食材。

前端行为：
- loading 期间禁用重复触发。
- 接口异常给出明确提示。
- 菜谱详情失败时提供“再次获取”。
- 时令建议接口异常时静默隐藏建议，不打断主流程。

## 11. 数据模型摘要

### 11.1 IngredientItem
- `id`
- `name`
- `normalizedName`
- `quantity`
- `source`
- `confidence`

### 11.2 RecipeRecommendation
- `id`
- `name`
- `namePinyin`
- `englishName`
- `nameLearning`
- `ageRange`
- `difficulty`
- `estimatedTimeMinutes`
- `riskAlerts`
- `nutritionSummary`
- `canCookWithCurrentIngredients`

### 11.3 RecipeDetail
继承 `RecipeRecommendation`，并增加：
- `prepTimeMinutes`
- `cookTimeMinutes`
- `ingredients`
- `steps`

### 11.4 CookingStep
- `id`
- `title`
- `description`
- `tip`
- `childAction`
- `parentAction`
- `expectedResult`
- `riskLevel`
- `requiresParentAssist`

### 11.5 ChatMessage
- `id`
- `role`
- `text`
- `createdAt`
- `imageDataUrl`
- `imageAlt`
- `ingredientsKey`
- `ingredients`
- `recipes`

## 12. 开发优先级
P0：
- Chatbox 主流程。
- 多模态食材输入。
- 食材清单确认。
- 推荐菜谱。
- 卡片内菜谱详情。
- 语音朗读。
- 菜谱收藏。
- 历史对话。
- 菜谱详情失败重试。

P1：
- 更精细的移动端手势体验。
- 更稳定的中文字典校验。
- 菜谱详情性能监控。
- 收藏菜谱跨设备同步。

P2：
- 笔顺动画。
- 成果点评增强。
- 账号体系。
- 社区分享。
- 成长体系。

## 13. 非 MVP 范围
- 不建设完整儿童档案管理模块。
- 不建设用户设置中心。
- 不建设服务端 TTS。
- 不建设真实菜谱图床。
- 不建设社区或公开分享。
- 不要求跨设备同步历史对话。
- 不要求离线模型能力。

## 14. 交付验收清单
- 用户能通过文字、语音、拍照和上传图片输入食材。
- 图片输入能在 chatbox 中展示图片预览。
- 识别食材后不会自动推荐菜谱。
- 食材清单支持横向滚动、朗读、删除和继续添加。
- 推荐按钮点击后不导致 chatbox 立即滚动。
- 推荐结果返回后，菜谱列表顶部位于用户视口内。
- 菜谱详情直接展示在卡片中。
- 菜谱无预览图。
- 菜谱中文名和英文名均可朗读。
- 步骤可朗读，并支持再次点击停止。
- 高风险步骤必须提示家长陪同。
- 菜谱详情失败时有 toast、失败提示和再次获取按钮。
- 历史对话和收藏菜谱可本地恢复。
- 移动端横滑食材和菜谱列表时尽量不触发浏览器后退。
