# Murphy's Cookbook 前端页面 PRD

## 1. 文档目的
本文档从前端产品实现角度，对 `requirements.md` 中的 MVP 范围进行页面级拆解，明确页面目标、信息结构、组件模块、交互状态和前端实现优先级。

## 2. 产品目标
- 让家长在最短路径内完成食材输入并获得可信推荐。
- 让儿童在视觉和文案上容易理解并愿意参与。
- 让高风险信息、过敏原过滤和适龄标签始终可见。

## 3. 页面总流程
1. 首页
2. 儿童档案页
3. 食材输入页
4. 识别确认页
5. 菜谱推荐列表页
6. 菜谱详情页
7. 分步烹饪页
8. 成果点评页

## 4. 全局设计要求

### 4.1 视觉方向
- 面向亲子场景，整体表达温暖、清晰、可信。
- 儿童内容使用更轻松的插画感语气，家长决策信息使用更稳重的信息卡表达。
- 重要提示采用显著色块区分：安全提示、过敏原过滤、推荐理由。

### 4.2 全局组件
- 顶部导航栏
- 当前儿童档案切换器
- 页面进度指示器
- 状态提示卡
- 主要按钮和次级按钮
- 风险提示条
- 推荐理由标签

### 4.3 通用状态
- 空态
- 加载态
- 识别中态
- 错误态
- 无结果态
- 完成态

## 5. 页面详细需求

### 5.1 首页
页面目标：
帮助用户快速开始一次推荐流程，并展示最近一次使用记录。

核心模块：
- 品牌区和产品说明
- 当前儿童档案卡片
- 三种食材输入入口
- 最近做过的菜谱
- 今日趣味挑战

关键交互：
- 点击档案卡进入档案页
- 点击拍照、语音或文本开始输入
- 点击最近菜谱进入详情

验收要点：
- 用户在首页能明确理解“输入食材即可推荐”
- 首屏必须能看到至少一个主行动按钮

### 5.2 儿童档案页
页面目标：
录入或编辑影响推荐结果的儿童信息。

核心模块：
- 昵称与年龄
- 口味偏好标签选择
- 过敏原标签选择
- 饮食习惯标签选择
- 保存按钮

关键交互：
- 支持标签多选
- 保存后返回首页或继续下一步

验收要点：
- 过敏原为必填提醒项
- 保存结果必须立即影响后续推荐

### 5.3 食材输入页
页面目标：
收集用户当前拥有的食材信息。

核心模块：
- 拍照上传区
- 语音输入区
- 文本录入区
- 常见食材快捷项
- 当前食材列表

关键交互：
- 支持混合输入
- 支持删除食材标签
- 支持继续补充食材

验收要点：
- 输入完成后用户能一键进入识别确认
- 若无食材，不能进入下一步

### 5.4 识别确认页
页面目标：
修正图片或语音识别后的食材结果，确保推荐输入可信。

核心模块：
- 识别出的食材清单
- 每个食材的编辑/删除操作
- 手动补充输入框
- 开始推荐按钮

关键交互：
- 用户可对不确定项进行修改
- 用户确认后进入推荐列表

验收要点：
- 系统必须允许人工修正识别误差
- 如果清单为空，给出返回补录入口

### 5.5 菜谱推荐列表页
页面目标：
输出个性化候选菜谱，并支持快速筛选。

核心模块：
- 推荐结果标题区
- 排序/筛选控件
- 菜谱卡片列表
- 双语菜谱名称区：中文名、英译名
- 菜名语音播报入口
- 推荐理由标签
- 适龄、难度、时长标签
- 无结果兜底提示

关键交互：
- 点击卡片进入详情
- 点击中文菜谱名时，从右侧滑出菜名识字抽屉，不在页面中默认展示识字内容
- 点击英译名时，播报英文菜名
- 在菜名识字抽屉中，点击任意汉字时播报该字的读音、笔画数、文字结构和一句儿童可理解的识字提示
- 点击抽屉关闭按钮或遮罩时关闭抽屉，并立即打断正在进行的语音播报
- 切换排序方式
- 返回上一步重新推荐

验收要点：
- 每个卡片必须有推荐理由
- 每个卡片必须同时展示中文菜谱名和英译名
- 中文名必须提供识字抽屉入口；英译名必须提供可点击播报入口；浏览器不支持语音时，识字信息仍需可见
- 拼音必须使用带调号形式，例如 `fān`、`qié`，不得在界面显示 `fan1`、`qie2`
- 过敏原过滤结果需要有可见说明

### 5.6 菜谱详情页
页面目标：
支持家长完成制作前判断。

核心模块：
- 菜谱头图和基础标签
- 中文菜谱名、英译名和菜名识字抽屉入口
- 食材与用量
- 营养摘要
- 安全提醒
- 步骤预览
- 开始制作按钮

关键交互：
- 展开查看全部步骤
- 点击开始制作进入烹饪页

验收要点：
- 高风险内容必须显著展示
- 食材与用量必须清晰
- 菜名识字内容默认不展示，必须在用户点击中文菜名后通过右侧抽屉展示

### 5.7 分步烹饪页
页面目标：
陪伴式完成料理过程。

核心模块：
- 当前步骤卡
- 步骤编号与进度
- 语音播报入口
- 小贴士与风险提醒
- 上一步/下一步按钮

关键交互：
- 切换步骤
- 在风险步骤显示家长协助提示
- 全部完成后进入成果点评页

验收要点：
- 儿童能明确知道当前是第几步
- 高风险步骤的视觉提示强于普通步骤

### 5.8 成果点评页
页面目标：
让用户上传成果并获得正向反馈。

核心模块：
- 成品图上传区
- 味道与体验反馈
- AI 点评结果卡
- 再试一道类似菜谱按钮
- 返回首页按钮

关键交互：
- 上传图片
- 生成点评
- 查看下一步建议

验收要点：
- 点评文案必须鼓励式
- 点评结果要包含优点和一条可操作建议

### 5.9 MVP 增量需求：双语菜名与识字播报
需求背景：
推荐菜谱不仅用于“吃什么”的决策，也承担儿童参与和学习兴趣引导。MVP 在不引入复杂课程系统的前提下，将菜谱名称作为轻量识字入口。

MVP 范围：
- 大模型推荐菜谱时，除中文菜谱名外，必须返回自然、准确、适合儿童理解的英文菜名。
- 大模型必须返回中文菜名中每个汉字的识字信息，包括：单字、拼音、笔画数、文字结构、儿童化说明。
- 拼音必须使用带调号拼音，而不是数字声调。
- 前端在推荐卡片和详情页展示中文名与英译名，识字详情不默认展开。
- 前端支持点击中文名打开右侧识字抽屉。
- 前端支持在抽屉内点击单个汉字，播报该字的拼音、笔画数、文字结构和说明。
- 前端支持用户中途打断语音播报：关闭识字抽屉时必须立即停止当前播报。
- 前端支持点击英译名播报英文发音。
- MVP 使用浏览器 Web Speech API 实现发音，不单独建设服务端 TTS。

非 MVP 范围：
- 不要求离线语音包。
- 不要求逐字高亮跟读。
- 不要求笔顺动画。
- 不要求多语言切换，英译名仅支持英文。
- 不要求默认展开识字内容。

风险与约束：
- Web Speech API 的音色、可用语言和发音质量取决于设备与浏览器。
- 大模型返回的笔画数需要通过结构化 schema 约束，后续可引入字典库校验。
- 英译名应优先保证自然表达，不逐字硬译。

## 6. 组件拆分建议
- `AppShell`
- `ProgressRail`
- `ProfileCard`
- `IngredientComposer`
- `IngredientTokenList`
- `RecipeCard`
- `RecipeNameSpeaker`
- `NameLearningCard`
- `NutritionCard`
- `SafetyAlert`
- `CookingStepCard`
- `FeedbackCard`

## 7. 前端数据模型建议
- `ChildProfile`
- `IngredientInput`
- `RecipeRecommendation`：包含 `name`、`englishName`、`nameLearning`
- `RecipeDetail`
- `CookingStep`
- `CookingFeedback`

## 8. 开发优先级
- P0：首页、档案页、输入页、确认页、推荐页、详情页、烹饪页、点评页
- P1：历史记录、收藏、个人中心
- P2：社区、成长体系、机构模式

## 9. 交付建议
前端 MVP 第一阶段建议先使用本地 mock 数据完成主流程闭环，等接口定义稳定后再接入服务端。
